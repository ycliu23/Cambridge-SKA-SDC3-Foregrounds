name: sdc3a
channels:
  - defaults
dependencies:
  - python=3.8
  - numpy
  - matplotlib
  - astropy
  - scipy
  - pip:
      - pandas==2.0.2
      - ps_eor
      - GPy
      - git+https://github.com/PolyChord/PolyChordLite@master
      - anesthetic
